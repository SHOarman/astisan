import 'package:flutter/material.dart';

class SerivesDetels extends StatelessWidget {
  const SerivesDetels({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
